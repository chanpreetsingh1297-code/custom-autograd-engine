# custom-autograd-engine
A lightweight, object-oriented automatic differentiation (Autograd) engine and neural network framework implemented from scratch in pure Python, featuring custom topological sorting and an O(1) space complexity inference context mode.
